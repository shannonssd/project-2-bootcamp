
  <%- include('nav', { link: "home-style.css" }); %>
  <body>
    <div class="table">

    
    <table>
      <tr>
        <th>Patient</th>
        <th>Relationship</th>
        <th>Date</th>
        <th>Hospital</th>
        <th>Department</th>
        <th>Time</th>
        <th>Edit</th>
        <th>Delete</th>
      </tr>
      <% for(let i = 0; i< apptData.length; i +=1){ %>
      <tr>
        <% for(let j = 0; j< 6; j +=1){ %>
        <th><%= apptData[i][j] %></th>
        <% } %>
        
        <th>
          <form action="/edit-appt">
            <label for="submit"></label>
            <input
              type="submit"
              id="submit"
              name="<%= `${[`${apptData[i][6]}`, `${apptData[i][7]}` ]}` %>"
              value="📝"
              onclick="return confirm('You can only change the date and time.');"
            />
          </form>
        </th>
        <th>
          <form action="/delete-appt">
            <label for="submit"></label>
            <input
              type="submit"
              id="submit"
              name="<%= apptData[i][6] %>"
              value="❌"
              onclick="return confirm('Are you sure you want to delete this appointment?');"
            />
          </form>
        </th>
      </tr>
      <% } %>
    </table>
  
    </div>
  </body>
</html>
