  <%- include('nav', { link: "/add-info-new-style.css" }); %>

  <body>
    <div class="main">
      <div>You have submitted the following:</div>
      <% newInfoArray.forEach((element) => { %>
      <div>
        <%= Object.keys(element)[0] %> : <%= Object.values(element)[0] %>
      </div>
      <% }); %>
      <button><a href="/">Home</a></button>
      <button><a href="/add-info">Add More Details</a></button>
    </div>
  </body>
</html>
